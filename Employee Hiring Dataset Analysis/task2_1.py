import numpy as np
import matplotlib.pyplot as plt
from settings import connect_database, load_records

#plot_filled_jobs predefined function returns the filled jobs and save the graph, you can view it in the plotting folder
#al the graph can be viewed in the plotting folder, we need to save the graph with respective tasks along with the naming convention
def plot_filled_jobs():
    #write your code here
    arrFilledjobs = None
    return arrFilledjobs

def task_runner():
    arrFilledjobs = plot_filled_jobs()
    print(arrFilledjobs)





